/**
 * TS mirror of the design tokens for any JS-side usage (e.g. theme-color meta,
 * inline gradients). Keep in sync with globals.css — that file remains the
 * single source of truth for styling.
 */
export const tokens = {
  brand: "#2599f6",
  brandTeal: "#07a9b4",
  ink900: "#131314",
  surface: "#fafafa",
  bg: "#ffffff",
} as const;
